<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.qms.mapper.QmsWorkshopMapper">

    <resultMap type="QmsWorkshop" id="QmsWorkshopResult">
        <result property="workshopId"    column="workshop_id"/>
        <result property="parentId"      column="parent_id"/>
        <result property="workshopCode"  column="workshop_code"/>
        <result property="workshopName"  column="workshop_name"/>
        <result property="nodeType"      column="node_type"/>
        <result property="orderNum"      column="order_num"/>
        <result property="status"        column="status"/>
        <result property="delFlag"       column="del_flag"/>
        <result property="createBy"      column="create_by"/>
        <result property="createTime"    column="create_time"/>
        <result property="updateBy"      column="update_by"/>
        <result property="updateTime"    column="update_time"/>
        <result property="remark"        column="remark"/>
    </resultMap>

    <sql id="selectWorkshopVo">
        SELECT workshop_id, parent_id, workshop_code, workshop_name, node_type, order_num,
               status, del_flag, create_by, create_time, update_by, update_time, remark
        FROM qms_workshop
    </sql>

    <select id="selectWorkshopList" parameterType="QmsWorkshop" resultMap="QmsWorkshopResult">
        <include refid="selectWorkshopVo"/>
        <where>
            del_flag = '0'
            <if test="workshopCode != null and workshopCode != ''"> AND workshop_code LIKE CONCAT('%', #{workshopCode}, '%')</if>
            <if test="workshopName != null and workshopName != ''"> AND workshop_name LIKE CONCAT('%', #{workshopName}, '%')</if>
            <if test="nodeType != null and nodeType != ''"> AND node_type = #{nodeType}</if>
            <if test="parentId != null"> AND parent_id = #{parentId}</if>
            <if test="status != null and status != ''"> AND status = #{status}</if>
        </where>
        ORDER BY parent_id, order_num
    </select>

    <select id="selectWorkshopById" parameterType="Long" resultMap="QmsWorkshopResult">
        <include refid="selectWorkshopVo"/>
        WHERE workshop_id = #{workshopId}
    </select>

    <select id="hasChildByWorkshopId" parameterType="Long" resultType="int">
        SELECT COUNT(1) FROM qms_workshop WHERE parent_id = #{workshopId} AND del_flag = '0'
    </select>

    <insert id="insertWorkshop" parameterType="QmsWorkshop" useGeneratedKeys="true" keyProperty="workshopId">
        INSERT INTO qms_workshop (
            parent_id, workshop_code, workshop_name, node_type, order_num,
            status, del_flag, create_by, create_time, remark
        ) VALUES (
            #{parentId}, #{workshopCode}, #{workshopName}, #{nodeType}, #{orderNum},
            #{status}, #{delFlag}, #{createBy}, sysdate(), #{remark}
        )
    </insert>

    <update id="updateWorkshop" parameterType="QmsWorkshop">
        UPDATE qms_workshop
        <set>
            <if test="parentId != null">parent_id = #{parentId},</if>
            <if test="workshopCode != null and workshopCode != ''">workshop_code = #{workshopCode},</if>
            <if test="workshopName != null and workshopName != ''">workshop_name = #{workshopName},</if>
            <if test="nodeType != null and nodeType != ''">node_type = #{nodeType},</if>
            <if test="orderNum != null">order_num = #{orderNum},</if>
            <if test="status != null and status != ''">status = #{status},</if>
            <if test="updateBy != null and updateBy != ''">update_by = #{updateBy},</if>
            <if test="remark != null">remark = #{remark},</if>
            update_time = sysdate()
        </set>
        WHERE workshop_id = #{workshopId}
    </update>

    <delete id="deleteWorkshopByIds" parameterType="Long">
        UPDATE qms_workshop SET del_flag = '2' WHERE workshop_id IN
        <foreach item="workshopId" collection="array" open="(" separator="," close=")">#{workshopId}</foreach>
    </delete>

</mapper>

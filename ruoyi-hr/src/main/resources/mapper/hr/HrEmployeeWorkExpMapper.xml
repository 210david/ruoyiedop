<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.hr.mapper.HrEmployeeWorkExpMapper">

    <resultMap type="HrEmployeeWorkExp" id="HrEmployeeWorkExpResult">
        <result property="workExpId" column="work_exp_id"/>
        <result property="employeeId" column="employee_id"/>
        <result property="startDate" column="start_date"/>
        <result property="endDate" column="end_date"/>
        <result property="companyName" column="company_name"/>
        <result property="position" column="position"/>
        <result property="workContent" column="work_content"/>
        <result property="leaveReason" column="leave_reason"/>
        <result property="status" column="status"/>
        <result property="delFlag" column="del_flag"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="remark" column="remark"/>
    </resultMap>

    <sql id="selectVo">
        SELECT work_exp_id, employee_id, start_date, end_date, company_name, position, work_content, leave_reason, status, del_flag, create_by, create_time, update_by, update_time, remark
        FROM biz_hr_employee_work_exp
    </sql>

    <select id="selectHrEmployeeWorkExpList" parameterType="HrEmployeeWorkExp" resultMap="HrEmployeeWorkExpResult">
        <include refid="selectVo"/>
        <where>
            (del_flag IS NULL OR del_flag = '0')
            <if test="employeeId != null"> AND employee_id = #{employeeId}</if>
            <if test="startDate != null"> AND start_date = #{startDate}</if>
            <if test="endDate != null"> AND end_date = #{endDate}</if>
            <if test="companyName != null and companyName != ''"> AND company_name LIKE CONCAT('%', #{companyName}, '%')</if>
            <if test="position != null and position != ''"> AND position LIKE CONCAT('%', #{position}, '%')</if>
            <if test="workContent != null and workContent != ''"> AND work_content LIKE CONCAT('%', #{workContent}, '%')</if>
            <if test="leaveReason != null and leaveReason != ''"> AND leave_reason LIKE CONCAT('%', #{leaveReason}, '%')</if>
            <if test="employeeId != null"> AND employee_id = #{employeeId}</if>
        </where>
        ORDER BY create_time DESC
    </select>

    <select id="selectHrEmployeeWorkExpByWorkExpId" parameterType="Long" resultMap="HrEmployeeWorkExpResult">
        <include refid="selectVo"/> WHERE work_exp_id = #{workExpId} AND (del_flag IS NULL OR del_flag = '0')
    </select>

    <insert id="insertHrEmployeeWorkExp" parameterType="HrEmployeeWorkExp" useGeneratedKeys="true" keyProperty="workExpId">
        INSERT INTO biz_hr_employee_work_exp (
            employee_id, start_date, end_date, company_name, position, work_content, leave_reason, status, del_flag, create_by, create_time, remark
        ) VALUES (
            #{employeeId}, #{startDate}, #{endDate}, #{companyName}, #{position}, #{workContent}, #{leaveReason}, #{status}, #{delFlag}, #{createBy}, sysdate(), #{remark}
        )
    </insert>

    <update id="updateHrEmployeeWorkExp" parameterType="HrEmployeeWorkExp">
        UPDATE biz_hr_employee_work_exp
        <set>
            <if test="employeeId != null">employee_id = #{employeeId},</if>
            <if test="startDate != null">start_date = #{startDate},</if>
            <if test="endDate != null">end_date = #{endDate},</if>
            <if test="companyName != null and companyName != ''">company_name = #{companyName},</if>
            <if test="position != null and position != ''">position = #{position},</if>
            <if test="workContent != null and workContent != ''">work_content = #{workContent},</if>
            <if test="leaveReason != null and leaveReason != ''">leave_reason = #{leaveReason},</if>
            <if test="status != null and status != ''">status = #{status},</if>
            <if test="updateBy != null and updateBy != ''">update_by = #{updateBy},</if>
            <if test="remark != null">remark = #{remark},</if>
            update_time = sysdate()
        </set>
        WHERE work_exp_id = #{workExpId}
    </update>

    <delete id="deleteHrEmployeeWorkExpByWorkExpId" parameterType="Long">
        UPDATE biz_hr_employee_work_exp SET del_flag = '2' WHERE work_exp_id = #{workExpId}
    </delete>

    <delete id="deleteHrEmployeeWorkExpByIds" parameterType="Long">
        UPDATE biz_hr_employee_work_exp SET del_flag = '2' WHERE work_exp_id IN
        <foreach item="workExpId" collection="array" open="(" separator="," close=")">#{workExpId}</foreach>
    </delete>

    <delete id="deleteHrEmployeeWorkExpByEmployeeId" parameterType="Long">
        UPDATE biz_hr_employee_work_exp SET del_flag = '2' WHERE employee_id = #{employeeId}
    </delete>

</mapper>

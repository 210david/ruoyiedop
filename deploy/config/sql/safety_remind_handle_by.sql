-- ============================================================
-- 到期提醒表新增「处理人」字段
-- 安全生产管理 / 到期提醒处理页面，记录处理人信息
-- ============================================================

ALTER TABLE biz_safety_remind
    ADD COLUMN handle_by VARCHAR(64) DEFAULT '' COMMENT '处理人' AFTER handle_time;

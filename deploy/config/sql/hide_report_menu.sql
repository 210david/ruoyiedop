-- =====================================================
-- 隐藏独立的"报工管理"菜单（已合并到派工管理Tab中）
-- 保留按钮权限，仅隐藏菜单入口（visible=1）
-- =====================================================

-- 隐藏报工管理菜单（menu_id=5120），按钮权限保留不动
UPDATE sys_menu SET visible = '1' WHERE menu_id = 5120;

-- 确认按钮权限仍然可用（不隐藏，保持 visible=0）
-- 5121~5125 为报工查询/新增/修改/删除/导出按钮，无需修改

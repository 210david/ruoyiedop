<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.safety.mapper.SafetyWorkerMapper">

    <resultMap type="SafetyWorker" id="SafetyWorkerResult">
        <result property="workerId"       column="worker_id"/>
        <result property="workerName"     column="worker_name"/>
        <result property="workerNo"       column="worker_no"/>
        <result property="deptId"         column="dept_id"/>
        <result property="deptName"       column="dept_name"/>
        <result property="position"       column="position"/>
        <result property="phone"          column="phone"/>
        <result property="isSpecial"      column="is_special"/>
        <result property="jobType"        column="job_type"/>
        <result property="certNo"         column="cert_no"/>
        <result property="certIssueDate"  column="cert_issue_date"/>
        <result property="validTo"        column="valid_to"/>
        <result property="reviewDate"     column="review_date"/>
        <result property="remindStatus"   column="remind_status"/>
        <result property="enterpriseId"   column="enterprise_id"/>
        <result property="status"         column="status"/>
        <result property="delFlag"        column="del_flag"/>
        <result property="createBy"       column="create_by"/>
        <result property="createTime"     column="create_time"/>
        <result property="updateBy"       column="update_by"/>
        <result property="updateTime"     column="update_time"/>
        <result property="remark"         column="remark"/>
    </resultMap>

    <sql id="selectVo">
        SELECT worker_id, worker_name, worker_no, dept_id, dept_name, position, phone,
               is_special, job_type, cert_no, cert_issue_date, valid_to, review_date,
               remind_status, enterprise_id, status, del_flag, create_by, create_time, update_by, update_time, remark
        FROM biz_safety_worker
    </sql>

    <select id="selectSafetyWorkerList" parameterType="SafetyWorker" resultMap="SafetyWorkerResult">
        <include refid="selectVo"/>
        <where>
            del_flag = '0'
            <if test="workerName != null and workerName != ''"> AND worker_name LIKE CONCAT('%', #{workerName}, '%')</if>
            <if test="workerNo != null and workerNo != ''"> AND worker_no = #{workerNo}</if>
            <if test="isSpecial != null and isSpecial != ''"> AND is_special = #{isSpecial}</if>
            <if test="jobType != null and jobType != ''"> AND job_type = #{jobType}</if>
            <if test="position != null and position != ''"> AND position LIKE CONCAT('%', #{position}, '%')</if>
            <if test="deptName != null and deptName != ''"> AND dept_name LIKE CONCAT('%', #{deptName}, '%')</if>
            <if test="phone != null and phone != ''"> AND phone LIKE CONCAT('%', #{phone}, '%')</if>
            <if test="status != null and status != ''"> AND status = #{status}</if>
        </where>
        ORDER BY create_time DESC
    </select>

    <select id="selectSafetyWorkerById" parameterType="Long" resultMap="SafetyWorkerResult">
        <include refid="selectVo"/> WHERE worker_id = #{workerId}
    </select>

    <insert id="insertSafetyWorker" parameterType="SafetyWorker" useGeneratedKeys="true" keyProperty="workerId">
        INSERT INTO biz_safety_worker (
            worker_name, worker_no, dept_id, dept_name, position, phone, is_special, job_type,
            cert_no, cert_issue_date, valid_to, review_date, remind_status, enterprise_id,
            status, create_by, create_time, remark
        ) VALUES (
            #{workerName}, #{workerNo}, #{deptId}, #{deptName}, #{position}, #{phone}, #{isSpecial}, #{jobType},
            #{certNo}, #{certIssueDate}, #{validTo}, #{reviewDate}, #{remindStatus}, #{enterpriseId},
            #{status}, #{createBy}, sysdate(), #{remark}
        )
    </insert>

    <update id="updateSafetyWorker" parameterType="SafetyWorker">
        UPDATE biz_safety_worker
        <set>
            <if test="workerName != null and workerName != ''">worker_name = #{workerName},</if>
            <if test="workerNo != null">worker_no = #{workerNo},</if>
            <if test="deptId != null">dept_id = #{deptId},</if>
            <if test="deptName != null">dept_name = #{deptName},</if>
            <if test="position != null">position = #{position},</if>
            <if test="phone != null">phone = #{phone},</if>
            <if test="isSpecial != null">is_special = #{isSpecial},</if>
            <if test="jobType != null">job_type = #{jobType},</if>
            <if test="certNo != null">cert_no = #{certNo},</if>
            <if test="certIssueDate != null">cert_issue_date = #{certIssueDate},</if>
            <if test="validTo != null">valid_to = #{validTo},</if>
            <if test="reviewDate != null">review_date = #{reviewDate},</if>
            <if test="remindStatus != null">remind_status = #{remindStatus},</if>
            <if test="enterpriseId != null">enterprise_id = #{enterpriseId},</if>
            <if test="status != null and status != ''">status = #{status},</if>
            <if test="updateBy != null and updateBy != ''">update_by = #{updateBy},</if>
            <if test="remark != null">remark = #{remark},</if>
            update_time = sysdate()
        </set>
        WHERE worker_id = #{workerId}
    </update>

    <delete id="deleteSafetyWorkerById" parameterType="Long">
        UPDATE biz_safety_worker SET del_flag = '2' WHERE worker_id = #{workerId}
    </delete>

    <delete id="deleteSafetyWorkerByIds" parameterType="Long">
        UPDATE biz_safety_worker SET del_flag = '2' WHERE worker_id IN
        <foreach item="workerId" collection="array" open="(" separator="," close=")">#{workerId}</foreach>
    </delete>

</mapper>
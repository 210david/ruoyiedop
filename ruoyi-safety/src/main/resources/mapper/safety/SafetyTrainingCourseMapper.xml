<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.safety.mapper.SafetyTrainingCourseMapper">

    <resultMap type="SafetyTrainingCourse" id="SafetyTrainingCourseResult">
        <result property="courseId"      column="course_id"/>
        <result property="courseCode"    column="course_code"/>
        <result property="courseName"    column="course_name"/>
        <result property="courseType"    column="course_type"/>
        <result property="courseForm"    column="course_form"/>
        <result property="hours"        column="hours"/>
        <result property="instructor"   column="instructor"/>
        <result property="description"  column="description"/>
        <result property="materialUrl"  column="material_url"/>
        <result property="enterpriseId"  column="enterprise_id"/>
        <result property="status"       column="status"/>
        <result property="delFlag"      column="del_flag"/>
        <result property="createBy"     column="create_by"/>
        <result property="createTime"   column="create_time"/>
        <result property="updateBy"     column="update_by"/>
        <result property="updateTime"   column="update_time"/>
        <result property="remark"       column="remark"/>
    </resultMap>

    <sql id="selectVo">
        SELECT course_id, course_code, course_name, course_type, course_form, hours, instructor,
               description, material_url, enterprise_id, status, del_flag,
               create_by, create_time, update_by, update_time, remark
        FROM biz_safety_training_course
    </sql>

    <select id="selectSafetyTrainingCourseList" parameterType="SafetyTrainingCourse" resultMap="SafetyTrainingCourseResult">
        <include refid="selectVo"/>
        <where>
            del_flag = '0'
            <if test="courseCode != null and courseCode != ''"> AND course_code LIKE CONCAT('%', #{courseCode}, '%')</if>
            <if test="courseName != null and courseName != ''"> AND course_name LIKE CONCAT('%', #{courseName}, '%')</if>
            <if test="courseType != null and courseType != ''"> AND course_type = #{courseType}</if>
            <if test="courseForm != null and courseForm != ''"> AND course_form = #{courseForm}</if>
            <if test="status != null and status != ''"> AND status = #{status}</if>
        </where>
        ORDER BY create_time DESC
    </select>

    <select id="selectSafetyTrainingCourseById" parameterType="Long" resultMap="SafetyTrainingCourseResult">
        <include refid="selectVo"/> WHERE course_id = #{courseId}
    </select>

    <insert id="insertSafetyTrainingCourse" parameterType="SafetyTrainingCourse" useGeneratedKeys="true" keyProperty="courseId">
        INSERT INTO biz_safety_training_course (
            course_code, course_name, course_type, course_form, hours, instructor,
            description, material_url, enterprise_id, status, create_by, create_time, remark
        ) VALUES (
            #{courseCode}, #{courseName}, #{courseType}, #{courseForm}, #{hours}, #{instructor},
            #{description}, #{materialUrl}, #{enterpriseId}, #{status}, #{createBy}, sysdate(), #{remark}
        )
    </insert>

    <update id="updateSafetyTrainingCourse" parameterType="SafetyTrainingCourse">
        UPDATE biz_safety_training_course
        <set>
            <if test="courseCode != null">course_code = #{courseCode},</if>
            <if test="courseName != null">course_name = #{courseName},</if>
            <if test="courseType != null">course_type = #{courseType},</if>
            <if test="courseForm != null">course_form = #{courseForm},</if>
            <if test="hours != null">hours = #{hours},</if>
            <if test="instructor != null">instructor = #{instructor},</if>
            <if test="description != null">description = #{description},</if>
            <if test="materialUrl != null">material_url = #{materialUrl},</if>
            <if test="enterpriseId != null">enterprise_id = #{enterpriseId},</if>
            <if test="status != null">status = #{status},</if>
            <if test="updateBy != null and updateBy != ''">update_by = #{updateBy},</if>
            <if test="remark != null">remark = #{remark},</if>
            update_time = sysdate()
        </set>
        WHERE course_id = #{courseId}
    </update>

    <delete id="deleteSafetyTrainingCourseById" parameterType="Long">
        UPDATE biz_safety_training_course SET del_flag = '2' WHERE course_id = #{courseId}
    </delete>

    <delete id="deleteSafetyTrainingCourseByIds" parameterType="Long">
        UPDATE biz_safety_training_course SET del_flag = '2' WHERE course_id IN
        <foreach item="courseId" collection="array" open="(" separator="," close=")">#{courseId}</foreach>
    </delete>

</mapper>

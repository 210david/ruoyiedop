<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.safety.mapper.SafetyMaterialMapper">

    <resultMap type="SafetyMaterial" id="SafetyMaterialResult">
        <result property="materialId"      column="material_id"/>
        <result property="materialCode"    column="material_code"/>
        <result property="materialName"    column="material_name"/>
        <result property="hazardClass"     column="hazard_class"/>
        <result property="casNo"           column="cas_no"/>
        <result property="maxStorage"      column="max_storage"/>
        <result property="storageUnit"     column="storage_unit"/>
        <result property="storageAreaId"   column="storage_area_id"/>
        <result property="storageAreaName" column="storage_area_name"/>
        <result property="sdsAttachment"   column="sds_attachment"/>
        <result property="toxicFlag"       column="toxic_flag"/>
        <result property="currentStock"    column="current_stock"/>
        <result property="enterpriseId"    column="enterprise_id"/>
        <result property="status"          column="status"/>
        <result property="delFlag"         column="del_flag"/>
        <result property="createBy"        column="create_by"/>
        <result property="createTime"      column="create_time"/>
        <result property="updateBy"        column="update_by"/>
        <result property="updateTime"      column="update_time"/>
        <result property="remark"          column="remark"/>
    </resultMap>

    <sql id="selectVo">
        SELECT material_id, material_code, material_name, hazard_class, cas_no, max_storage, storage_unit,
               storage_area_id, storage_area_name, sds_attachment, toxic_flag, current_stock,
               enterprise_id, status, del_flag, create_by, create_time, update_by, update_time, remark
        FROM biz_safety_material
    </sql>

    <select id="selectSafetyMaterialList" parameterType="SafetyMaterial" resultMap="SafetyMaterialResult">
        <include refid="selectVo"/>
        <where>
            del_flag = '0'
            <if test="materialCode != null and materialCode != ''"> AND material_code LIKE CONCAT('%', #{materialCode}, '%')</if>
            <if test="materialName != null and materialName != ''"> AND material_name LIKE CONCAT('%', #{materialName}, '%')</if>
            <if test="hazardClass != null and hazardClass != ''"> AND hazard_class = #{hazardClass}</if>
            <if test="toxicFlag != null and toxicFlag != ''"> AND toxic_flag = #{toxicFlag}</if>
            <if test="status != null and status != ''"> AND status = #{status}</if>
        </where>
        ORDER BY create_time DESC
    </select>

    <select id="selectSafetyMaterialById" parameterType="Long" resultMap="SafetyMaterialResult">
        <include refid="selectVo"/> WHERE material_id = #{materialId}
    </select>

    <select id="selectSafetyMaterialByCode" parameterType="String" resultMap="SafetyMaterialResult">
        <include refid="selectVo"/> WHERE material_code = #{materialCode} AND del_flag = '0'
    </select>

    <insert id="insertSafetyMaterial" parameterType="SafetyMaterial" useGeneratedKeys="true" keyProperty="materialId">
        INSERT INTO biz_safety_material (
            material_code, material_name, hazard_class, cas_no, max_storage, storage_unit,
            storage_area_id, storage_area_name, sds_attachment, toxic_flag, current_stock,
            enterprise_id, status, create_by, create_time, remark
        ) VALUES (
            #{materialCode}, #{materialName}, #{hazardClass}, #{casNo}, #{maxStorage}, #{storageUnit},
            #{storageAreaId}, #{storageAreaName}, #{sdsAttachment}, #{toxicFlag}, #{currentStock},
            #{enterpriseId}, #{status}, #{createBy}, sysdate(), #{remark}
        )
    </insert>

    <update id="updateSafetyMaterial" parameterType="SafetyMaterial">
        UPDATE biz_safety_material
        <set>
            <if test="materialCode != null and materialCode != ''">material_code = #{materialCode},</if>
            <if test="materialName != null and materialName != ''">material_name = #{materialName},</if>
            <if test="hazardClass != null">hazard_class = #{hazardClass},</if>
            <if test="casNo != null">cas_no = #{casNo},</if>
            <if test="maxStorage != null">max_storage = #{maxStorage},</if>
            <if test="storageUnit != null">storage_unit = #{storageUnit},</if>
            <if test="storageAreaId != null">storage_area_id = #{storageAreaId},</if>
            <if test="storageAreaName != null">storage_area_name = #{storageAreaName},</if>
            <if test="sdsAttachment != null">sds_attachment = #{sdsAttachment},</if>
            <if test="toxicFlag != null">toxic_flag = #{toxicFlag},</if>
            <if test="currentStock != null">current_stock = #{currentStock},</if>
            <if test="enterpriseId != null">enterprise_id = #{enterpriseId},</if>
            <if test="status != null and status != ''">status = #{status},</if>
            <if test="updateBy != null and updateBy != ''">update_by = #{updateBy},</if>
            <if test="remark != null">remark = #{remark},</if>
            update_time = sysdate()
        </set>
        WHERE material_id = #{materialId}
    </update>

    <delete id="deleteSafetyMaterialById" parameterType="Long">
        UPDATE biz_safety_material SET del_flag = '2' WHERE material_id = #{materialId}
    </delete>

    <delete id="deleteSafetyMaterialByIds" parameterType="Long">
        UPDATE biz_safety_material SET del_flag = '2' WHERE material_id IN
        <foreach item="materialId" collection="array" open="(" separator="," close=")">#{materialId}</foreach>
    </delete>

</mapper>
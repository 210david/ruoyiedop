<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.safety.mapper.SafetyEnterpriseMapper">

    <resultMap type="SafetyEnterprise" id="SafetyEnterpriseResult">
        <result property="enterpriseId"   column="enterprise_id"/>
        <result property="enterpriseName" column="enterprise_name"/>
        <result property="creditCode"     column="credit_code"/>
        <result property="industryType"   column="industry_type"/>
        <result property="isHighRisk"     column="is_high_risk"/>
        <result property="legalPerson"    column="legal_person"/>
        <result property="safetyManagerId" column="safety_manager_id"/>
        <result property="safetyManager"  column="safety_manager"/>
        <result property="safetyOrg"      column="safety_org"/>
        <result property="responsibility" column="responsibility"/>
        <result property="superviseDept"  column="supervise_dept"/>
        <result property="planImage"      column="plan_image"/>
        <result property="status"         column="status"/>
        <result property="delFlag"        column="del_flag"/>
        <result property="createBy"       column="create_by"/>
        <result property="createTime"     column="create_time"/>
        <result property="updateBy"       column="update_by"/>
        <result property="updateTime"     column="update_time"/>
        <result property="remark"         column="remark"/>
    </resultMap>

    <sql id="selectVo">
        SELECT enterprise_id, enterprise_name, credit_code, industry_type, is_high_risk, legal_person,
               safety_manager_id, safety_manager, safety_org, responsibility, supervise_dept, plan_image,
               status, del_flag, create_by, create_time, update_by, update_time, remark
        FROM biz_safety_enterprise
    </sql>

    <select id="selectSafetyEnterpriseList" parameterType="SafetyEnterprise" resultMap="SafetyEnterpriseResult">
        <include refid="selectVo"/>
        <where>
            del_flag = '0'
            <if test="enterpriseName != null and enterpriseName != ''"> AND enterprise_name LIKE CONCAT('%', #{enterpriseName}, '%')</if>
            <if test="creditCode != null and creditCode != ''"> AND credit_code = #{creditCode}</if>
            <if test="industryType != null and industryType != ''"> AND industry_type = #{industryType}</if>
            <if test="isHighRisk != null and isHighRisk != ''"> AND is_high_risk = #{isHighRisk}</if>
            <if test="status != null and status != ''"> AND status = #{status}</if>
        </where>
        ORDER BY create_time DESC
    </select>

    <select id="selectSafetyEnterpriseById" parameterType="Long" resultMap="SafetyEnterpriseResult">
        <include refid="selectVo"/> WHERE enterprise_id = #{enterpriseId}
    </select>

    <insert id="insertSafetyEnterprise" parameterType="SafetyEnterprise" useGeneratedKeys="true" keyProperty="enterpriseId">
        INSERT INTO biz_safety_enterprise (
            enterprise_name, credit_code, industry_type, is_high_risk, legal_person,
            safety_manager_id, safety_manager, safety_org, responsibility, supervise_dept, plan_image,
            status, create_by, create_time, remark
        ) VALUES (
            #{enterpriseName}, #{creditCode}, #{industryType}, #{isHighRisk}, #{legalPerson},
            #{safetyManagerId}, #{safetyManager}, #{safetyOrg}, #{responsibility}, #{superviseDept}, #{planImage},
            #{status}, #{createBy}, sysdate(), #{remark}
        )
    </insert>

    <update id="updateSafetyEnterprise" parameterType="SafetyEnterprise">
        UPDATE biz_safety_enterprise
        <set>
            <if test="enterpriseName != null and enterpriseName != ''">enterprise_name = #{enterpriseName},</if>
            <if test="creditCode != null and creditCode != ''">credit_code = #{creditCode},</if>
            <if test="industryType != null and industryType != ''">industry_type = #{industryType},</if>
            <if test="isHighRisk != null and isHighRisk != ''">is_high_risk = #{isHighRisk},</if>
            <if test="legalPerson != null">legal_person = #{legalPerson},</if>
            <if test="safetyManagerId != null">safety_manager_id = #{safetyManagerId},</if>
            <if test="safetyManager != null">safety_manager = #{safetyManager},</if>
            <if test="safetyOrg != null">safety_org = #{safetyOrg},</if>
            <if test="responsibility != null">responsibility = #{responsibility},</if>
            <if test="superviseDept != null">supervise_dept = #{superviseDept},</if>
            <if test="planImage != null">plan_image = #{planImage},</if>
            <if test="status != null and status != ''">status = #{status},</if>
            <if test="updateBy != null and updateBy != ''">update_by = #{updateBy},</if>
            <if test="remark != null">remark = #{remark},</if>
            update_time = sysdate()
        </set>
        WHERE enterprise_id = #{enterpriseId}
    </update>

    <delete id="deleteSafetyEnterpriseById" parameterType="Long">
        UPDATE biz_safety_enterprise SET del_flag = '2' WHERE enterprise_id = #{enterpriseId}
    </delete>

    <delete id="deleteSafetyEnterpriseByIds" parameterType="Long">
        UPDATE biz_safety_enterprise SET del_flag = '2' WHERE enterprise_id IN
        <foreach item="enterpriseId" collection="array" open="(" separator="," close=")">#{enterpriseId}</foreach>
    </delete>

</mapper>
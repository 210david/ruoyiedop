<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.safety.mapper.SafetyEmergencyPlanMapper">

    <resultMap type="SafetyEmergencyPlan" id="SafetyEmergencyPlanResult">
        <result property="planId"         column="plan_id"/>
        <result property="planCode"       column="plan_code"/>
        <result property="planName"       column="plan_name"/>
        <result property="planType"       column="plan_type"/>
        <result property="planLevel"      column="plan_level"/>
        <result property="version"        column="version"/>
        <result property="regNo"          column="reg_no"/>
        <result property="regDate"        column="reg_date"/>
        <result property="effectiveDate"  column="effective_date"/>
        <result property="expireDate"     column="expire_date"/>
        <result property="remindStatus"   column="remind_status"/>
        <result property="content"        column="content"/>
        <result property="attachment"     column="attachment"/>
        <result property="enterpriseId"   column="enterprise_id"/>
        <result property="status"         column="status"/>
        <result property="delFlag"        column="del_flag"/>
        <result property="createBy"       column="create_by"/>
        <result property="createTime"     column="create_time"/>
        <result property="updateBy"       column="update_by"/>
        <result property="updateTime"     column="update_time"/>
        <result property="remark"         column="remark"/>
    </resultMap>

    <sql id="selectVo">
        SELECT plan_id, plan_code, plan_name, plan_type, plan_level, version, reg_no, reg_date,
               effective_date, expire_date, remind_status, content, attachment, enterprise_id,
               status, del_flag, create_by, create_time, update_by, update_time, remark
        FROM biz_safety_emergency_plan
    </sql>

    <select id="selectSafetyEmergencyPlanList" parameterType="SafetyEmergencyPlan" resultMap="SafetyEmergencyPlanResult">
        <include refid="selectVo"/>
        <where>
            del_flag = '0'
            <if test="planCode != null and planCode != ''"> AND plan_code LIKE CONCAT('%', #{planCode}, '%')</if>
            <if test="planName != null and planName != ''"> AND plan_name LIKE CONCAT('%', #{planName}, '%')</if>
            <if test="planType != null and planType != ''"> AND plan_type = #{planType}</if>
            <if test="planLevel != null and planLevel != ''"> AND plan_level = #{planLevel}</if>
            <if test="status != null and status != ''"> AND status = #{status}</if>
            <if test="version != null and version != ''"> AND version LIKE CONCAT('%', #{version}, '%')</if>
            <if test="regNo != null and regNo != ''"> AND reg_no LIKE CONCAT('%', #{regNo}, '%')</if>
            <if test="params.beginEffectiveDate != null and params.beginEffectiveDate != ''"> AND effective_date &gt;= #{params.beginEffectiveDate}</if>
            <if test="params.endEffectiveDate != null and params.endEffectiveDate != ''"> AND effective_date &lt;= #{params.endEffectiveDate}</if>
        </where>
        ORDER BY create_time DESC
    </select>

    <select id="selectSafetyEmergencyPlanById" parameterType="Long" resultMap="SafetyEmergencyPlanResult">
        <include refid="selectVo"/> WHERE plan_id = #{planId}
    </select>

    <insert id="insertSafetyEmergencyPlan" parameterType="SafetyEmergencyPlan" useGeneratedKeys="true" keyProperty="planId">
        INSERT INTO biz_safety_emergency_plan (
            plan_code, plan_name, plan_type, plan_level, version, reg_no, reg_date,
            effective_date, expire_date, remind_status, content, attachment, enterprise_id,
            status, create_by, create_time, remark
        ) VALUES (
            #{planCode}, #{planName}, #{planType}, #{planLevel}, #{version}, #{regNo}, #{regDate},
            #{effectiveDate}, #{expireDate}, #{remindStatus}, #{content}, #{attachment}, #{enterpriseId},
            #{status}, #{createBy}, sysdate(), #{remark}
        )
    </insert>

    <update id="updateSafetyEmergencyPlan" parameterType="SafetyEmergencyPlan">
        UPDATE biz_safety_emergency_plan
        <set>
            <if test="planCode != null and planCode != ''">plan_code = #{planCode},</if>
            <if test="planName != null and planName != ''">plan_name = #{planName},</if>
            <if test="planType != null">plan_type = #{planType},</if>
            <if test="planLevel != null">plan_level = #{planLevel},</if>
            <if test="version != null">version = #{version},</if>
            <if test="regNo != null">reg_no = #{regNo},</if>
            <if test="regDate != null">reg_date = #{regDate},</if>
            <if test="effectiveDate != null">effective_date = #{effectiveDate},</if>
            <if test="expireDate != null">expire_date = #{expireDate},</if>
            <if test="remindStatus != null">remind_status = #{remindStatus},</if>
            <if test="content != null">content = #{content},</if>
            <if test="attachment != null">attachment = #{attachment},</if>
            <if test="enterpriseId != null">enterprise_id = #{enterpriseId},</if>
            <if test="status != null and status != ''">status = #{status},</if>
            <if test="updateBy != null and updateBy != ''">update_by = #{updateBy},</if>
            <if test="remark != null">remark = #{remark},</if>
            update_time = sysdate()
        </set>
        WHERE plan_id = #{planId}
    </update>

    <delete id="deleteSafetyEmergencyPlanById" parameterType="Long">
        UPDATE biz_safety_emergency_plan SET del_flag = '2' WHERE plan_id = #{planId}
    </delete>

    <delete id="deleteSafetyEmergencyPlanByIds" parameterType="Long">
        UPDATE biz_safety_emergency_plan SET del_flag = '2' WHERE plan_id IN
        <foreach item="planId" collection="array" open="(" separator="," close=")">#{planId}</foreach>
    </delete>

</mapper>
<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.mms.mapper.MmsShiftMapper">

    <resultMap type="MmsShift" id="MmsShiftResult">
                <id property="shiftId" column="shift_id"/>
<result property="shiftCode"     column="shift_code"/>
<result property="shiftName"     column="shift_name"/>
        <result property="startTime"     column="start_time"/>
        <result property="endTime"     column="end_time"/>
        <result property="restMinutes"     column="rest_minutes"/>
        <result property="availHours"     column="avail_hours"/>
        <result property="status"     column="status"/>
        <result property="delFlag"     column="del_flag"/>
        <result property="createBy"     column="create_by"/>
        <result property="createTime"     column="create_time"/>
        <result property="updateBy"     column="update_by"/>
        <result property="updateTime"     column="update_time"/>
        <result property="remark"     column="remark"/>
    </resultMap>

    <sql id="selectShiftVo">
        SELECT shift_id, shift_code, shift_name, start_time, end_time, rest_minutes, avail_hours, status, del_flag, create_by, create_time, update_by, update_time, remark
        FROM mms_shift
    </sql>

    <select id="selectShiftList" parameterType="MmsShift" resultMap="MmsShiftResult">
        <include refid="selectShiftVo"/>
        <where>
            del_flag = '0'
            <if test="shiftCode != null and shiftCode != ''"> AND shift_code LIKE CONCAT('%', #{shiftCode}, '%')</if>
            <if test="shiftName != null and shiftName != ''"> AND shift_name LIKE CONCAT('%', #{shiftName}, '%')</if>
            <if test="status != null and status != ''"> AND status = #{status}</if>
            <if test="params.beginTime != null and params.beginTime != ''"> AND date(create_time) &gt;= #{params.beginTime}</if>
            <if test="params.endTime != null and params.endTime != ''"> AND date(create_time) &lt;= #{params.endTime}</if>
        </where>
        ORDER BY shift_id DESC
    </select>

    <select id="selectShiftById" parameterType="Long" resultMap="MmsShiftResult">
        <include refid="selectShiftVo"/>
        WHERE shift_id = #{shiftId}
    </select>

    <insert id="insertShift" parameterType="MmsShift" useGeneratedKeys="true" keyProperty="shiftId">
        INSERT INTO mms_shift (
            shift_code,
            shift_name,
            start_time,
            end_time,
            rest_minutes,
            avail_hours,
            status,
            del_flag,
            create_by,
            create_time,
            update_by,
            update_time,
            remark
        )VALUES(
            #{shiftCode},
            #{shiftName},
            #{startTime},
            #{endTime},
            #{restMinutes},
            #{availHours},
            #{status},
            #{delFlag},
            #{createBy},
            #{createTime},
            #{updateBy},
            #{updateTime},
            #{remark}
        )
    </insert>

    <update id="updateShift" parameterType="MmsShift">
        UPDATE mms_shift SET
            shift_name = #{shiftName},
            start_time = #{startTime},
            end_time = #{endTime},
            rest_minutes = #{restMinutes},
            avail_hours = #{availHours},
            status = #{status},
            update_by = #{updateBy},
            update_time = #{updateTime},
            remark = #{remark}
        WHERE shift_id = #{shiftId}
    </update>

    <delete id="deleteShiftByIds" parameterType="Long">
        UPDATE mms_shift SET del_flag = '2' WHERE shift_id IN
        <foreach collection="array" item="shiftId" open="(" separator="," close=")">
            #{shiftId}
        </foreach>
    </delete>
</mapper>

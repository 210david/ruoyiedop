<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.mms.mapper.MmsLineMapper">

    <resultMap type="MmsLine" id="MmsLineResult">
        <id property="lineId" column="line_id"/>
        <result property="lineCode" column="line_code"/>
        <result property="lineName" column="line_name"/>
        <result property="workshopId" column="workshop_id"/>
        <result property="lineType" column="line_type"/>
        <result property="cycleTime" column="cycle_time"/>
        <result property="status" column="status"/>
        <result property="delFlag" column="del_flag"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="remark" column="remark"/>
        <result property="workshopName" column="workshop_name"/>
    </resultMap>

    <sql id="selectLineVo">
        SELECT l.line_id, l.line_code, l.line_name, l.workshop_id, l.line_type, l.cycle_time, l.status, l.del_flag,
               l.create_by, l.create_time, l.update_by, l.update_time, l.remark,
               w.workshop_name
        FROM mms_line l
        LEFT JOIN mms_workshop w ON l.workshop_id = w.workshop_id
    </sql>

    <select id="selectLineList" parameterType="MmsLine" resultMap="MmsLineResult">
        <include refid="selectLineVo"/>
        <where>
            l.del_flag = '0'
            <if test="lineCode != null and lineCode != ''"> AND l.line_code LIKE CONCAT('%', #{lineCode}, '%')</if>
            <if test="lineName != null and lineName != ''"> AND l.line_name LIKE CONCAT('%', #{lineName}, '%')</if>
            <if test="workshopId != null"> AND l.workshop_id = #{workshopId}</if>
            <if test="lineType != null and lineType != ''"> AND l.line_type = #{lineType}</if>
            <if test="status != null and status != ''"> AND l.status = #{status}</if>
        </where>
        ORDER BY l.line_id ASC
    </select>

    <select id="selectLineById" parameterType="Long" resultMap="MmsLineResult">
        <include refid="selectLineVo"/>
        WHERE l.line_id = #{lineId}
    </select>

    <insert id="insertLine" parameterType="MmsLine" useGeneratedKeys="true" keyProperty="lineId">
        INSERT INTO mms_line (
            line_code, line_name, workshop_id, line_type, cycle_time, status, del_flag, create_by, create_time, update_by, update_time, remark
        ) VALUES (
            #{lineCode}, #{lineName}, #{workshopId}, #{lineType}, #{cycleTime}, #{status}, #{delFlag}, #{createBy}, sysdate(), #{updateBy}, #{updateTime}, #{remark}
        )
    </insert>

    <update id="updateLine" parameterType="MmsLine">
        UPDATE mms_line SET
            line_code = #{lineCode},
            line_name = #{lineName},
            workshop_id = #{workshopId},
            line_type = #{lineType},
            cycle_time = #{cycleTime},
            status = #{status},
            update_by = #{updateBy},
            update_time = sysdate(),
            remark = #{remark}
        WHERE line_id = #{lineId}
    </update>

    <delete id="deleteLineByIds" parameterType="Long">
        UPDATE mms_line SET del_flag = '2' WHERE line_id IN
        <foreach collection="array" item="lineId" open="(" separator="," close=")">
            #{lineId}
        </foreach>
    </delete>
</mapper>

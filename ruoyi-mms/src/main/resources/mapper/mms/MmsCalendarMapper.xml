<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.mms.mapper.MmsCalendarMapper">

    <resultMap type="MmsCalendar" id="MmsCalendarResult">
        <id property="calendarId" column="calendar_id"/>
        <result property="workDate" column="work_date"/>
        <result property="dayType" column="day_type"/>
        <result property="shiftJson" column="shift_json"/>
        <result property="availHours" column="avail_hours"/>
        <result property="status" column="status"/>
        <result property="delFlag" column="del_flag"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="remark" column="remark"/>
    </resultMap>

    <sql id="selectCalendarVo">
        SELECT calendar_id, work_date, day_type, shift_json, avail_hours, status, del_flag, create_by, create_time, update_by, update_time, remark
        FROM mms_factory_calendar
    </sql>

    <select id="selectCalendarList" parameterType="MmsCalendar" resultMap="MmsCalendarResult">
        <include refid="selectCalendarVo"/>
        <where>
            del_flag = '0'
            <if test="dayType != null and dayType != ''"> AND day_type = #{dayType}</if>
            <if test="status != null and status != ''"> AND status = #{status}</if>
            <if test="params.beginTime != null and params.beginTime != ''"> AND work_date &gt;= #{params.beginTime}</if>
            <if test="params.endTime != null and params.endTime != ''"> AND work_date &lt;= #{params.endTime}</if>
        </where>
        ORDER BY work_date DESC
    </select>

    <select id="selectCalendarById" parameterType="Long" resultMap="MmsCalendarResult">
        <include refid="selectCalendarVo"/>
        WHERE calendar_id = #{calendarId}
    </select>

    <insert id="insertCalendar" parameterType="MmsCalendar" useGeneratedKeys="true" keyProperty="calendarId">
        INSERT INTO mms_factory_calendar (
            work_date, day_type, shift_json, avail_hours, status, del_flag, create_by, create_time, update_by, update_time, remark
        ) VALUES (
            #{workDate}, #{dayType}, #{shiftJson}, #{availHours}, #{status}, #{delFlag}, #{createBy}, sysdate(), #{updateBy}, #{updateTime}, #{remark}
        )
    </insert>

    <update id="updateCalendar" parameterType="MmsCalendar">
        UPDATE mms_factory_calendar SET
            work_date = #{workDate},
            day_type = #{dayType},
            shift_json = #{shiftJson},
            avail_hours = #{availHours},
            status = #{status},
            update_by = #{updateBy},
            update_time = sysdate(),
            remark = #{remark}
        WHERE calendar_id = #{calendarId}
    </update>

    <delete id="deleteCalendarByIds" parameterType="Long">
        UPDATE mms_factory_calendar SET del_flag = '2' WHERE calendar_id IN
        <foreach collection="array" item="calendarId" open="(" separator="," close=")">
            #{calendarId}
        </foreach>
    </delete>
</mapper>

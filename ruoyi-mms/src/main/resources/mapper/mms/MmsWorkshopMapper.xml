<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.mms.mapper.MmsWorkshopMapper">

    <resultMap type="MmsWorkshop" id="MmsWorkshopResult">
        <id property="workshopId" column="workshop_id"/>
        <result property="workshopCode" column="workshop_code"/>
        <result property="workshopName" column="workshop_name"/>
        <result property="factoryId" column="factory_id"/>
        <result property="leaderUserId" column="leader_user_id"/>
        <result property="status" column="status"/>
        <result property="delFlag" column="del_flag"/>
        <result property="createBy" column="create_by"/>
        <result property="createTime" column="create_time"/>
        <result property="updateBy" column="update_by"/>
        <result property="updateTime" column="update_time"/>
        <result property="remark" column="remark"/>
        <result property="factoryName" column="factory_name"/>
    </resultMap>

    <sql id="selectWorkshopVo">
        SELECT w.workshop_id, w.workshop_code, w.workshop_name, w.factory_id, w.leader_user_id, w.status, w.del_flag,
               w.create_by, w.create_time, w.update_by, w.update_time, w.remark,
               f.factory_name
        FROM mms_workshop w
        LEFT JOIN mms_factory f ON w.factory_id = f.factory_id
    </sql>

    <select id="selectWorkshopList" parameterType="MmsWorkshop" resultMap="MmsWorkshopResult">
        <include refid="selectWorkshopVo"/>
        <where>
            w.del_flag = '0'
            <if test="workshopCode != null and workshopCode != ''"> AND w.workshop_code LIKE CONCAT('%', #{workshopCode}, '%')</if>
            <if test="workshopName != null and workshopName != ''"> AND w.workshop_name LIKE CONCAT('%', #{workshopName}, '%')</if>
            <if test="factoryId != null"> AND w.factory_id = #{factoryId}</if>
            <if test="status != null and status != ''"> AND w.status = #{status}</if>
        </where>
        ORDER BY w.workshop_id ASC
    </select>

    <select id="selectWorkshopById" parameterType="Long" resultMap="MmsWorkshopResult">
        <include refid="selectWorkshopVo"/>
        WHERE w.workshop_id = #{workshopId}
    </select>

    <insert id="insertWorkshop" parameterType="MmsWorkshop" useGeneratedKeys="true" keyProperty="workshopId">
        INSERT INTO mms_workshop (
            workshop_code, workshop_name, factory_id, leader_user_id, status, del_flag, create_by, create_time, update_by, update_time, remark
        ) VALUES (
            #{workshopCode}, #{workshopName}, #{factoryId}, #{leaderUserId}, #{status}, #{delFlag}, #{createBy}, sysdate(), #{updateBy}, #{updateTime}, #{remark}
        )
    </insert>

    <update id="updateWorkshop" parameterType="MmsWorkshop">
        UPDATE mms_workshop SET
            workshop_code = #{workshopCode},
            workshop_name = #{workshopName},
            factory_id = #{factoryId},
            leader_user_id = #{leaderUserId},
            status = #{status},
            update_by = #{updateBy},
            update_time = sysdate(),
            remark = #{remark}
        WHERE workshop_id = #{workshopId}
    </update>

    <delete id="deleteWorkshopByIds" parameterType="Long">
        UPDATE mms_workshop SET del_flag = '2' WHERE workshop_id IN
        <foreach collection="array" item="workshopId" open="(" separator="," close=")">
            #{workshopId}
        </foreach>
    </delete>
</mapper>

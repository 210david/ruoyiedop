<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.mms.mapper.MmsDemandMapper">

    <resultMap type="MmsDemand" id="MmsDemandResult">
                <id property="demandId" column="demand_id"/>
<result property="demandNo"     column="demand_no"/>
        <result property="demandType"     column="demand_type"/>
        <result property="sourceOrderNo"     column="source_order_no"/>
        <result property="productId"     column="product_id"/>
        <result property="productCode"     column="product_code"/>
        <result property="productName"     column="product_name"/>
        <result property="specModel"     column="spec_model"/>
        <result property="unit"     column="unit"/>
        <result property="demandQty"     column="demand_qty"/>
        <result property="requireDate"     column="require_date"/>
        <result property="priority"     column="priority"/>
        <result property="customerName"     column="customer_name"/>
        <result property="status"     column="status"/>
        <result property="delFlag"     column="del_flag"/>
        <result property="createBy"     column="create_by"/>
        <result property="createTime"     column="create_time"/>
        <result property="updateBy"     column="update_by"/>
        <result property="updateTime"     column="update_time"/>
        <result property="remark"     column="remark"/>
    </resultMap>

    <sql id="selectDemandVo">
        SELECT demand_id, demand_no, demand_type, source_order_no, product_id, product_code, product_name, spec_model, unit, demand_qty, require_date, priority, customer_name, status, del_flag, create_by, create_time, update_by, update_time, remark
        FROM mms_demand
    </sql>

    <select id="selectDemandList" parameterType="MmsDemand" resultMap="MmsDemandResult">
        <include refid="selectDemandVo"/>
        <where>
            del_flag = '0'
            <if test="demandNo != null and demandNo != ''"> AND demand_no LIKE CONCAT('%', #{demand_no}, '%')</if>
            <if test="demandType != null and demandType != ''"> AND demand_type = #{demand_type}</if>
            <if test="productCode != null and productCode != ''"> AND product_code LIKE CONCAT('%', #{product_code}, '%')</if>
            <if test="productName != null and productName != ''"> AND product_name LIKE CONCAT('%', #{product_name}, '%')</if>
            <if test="status != null and status != ''"> AND status = #{status}</if>
            <if test="priority != null and priority != ''"> AND priority = #{priority}</if>
            <if test="customerName != null and customerName != ''"> AND customer_name LIKE CONCAT('%', #{customer_name}, '%')</if>
            <if test="params.beginTime != null and params.beginTime != ''"> AND date(create_time) &gt;= #{params.beginTime}</if>
            <if test="params.endTime != null and params.endTime != ''"> AND date(create_time) &lt;= #{params.endTime}</if>
        </where>
        ORDER BY demand_id DESC
    </select>

    <select id="selectDemandById" parameterType="Long" resultMap="MmsDemandResult">
        <include refid="selectDemandVo"/>
        WHERE demand_id = #{demandId}
    </select>

    <insert id="insertDemand" parameterType="MmsDemand" useGeneratedKeys="true" keyProperty="demandId">
        INSERT INTO mms_demand (
            demand_no,
            demand_type,
            source_order_no,
            product_id,
            product_code,
            product_name,
            spec_model,
            unit,
            demand_qty,
            require_date,
            priority,
            customer_name,
            status,
            del_flag,
            create_by,
            create_time,
            update_by,
            update_time,
            remark
        )VALUES(
            #{demandNo},
            #{demandType},
            #{sourceOrderNo},
            #{productId},
            #{productCode},
            #{productName},
            #{specModel},
            #{unit},
            #{demandQty},
            #{requireDate},
            #{priority},
            #{customerName},
            #{status},
            #{delFlag},
            #{createBy},
            #{createTime},
            #{updateBy},
            #{updateTime},
            #{remark}
        )
    </insert>

    <update id="updateDemand" parameterType="MmsDemand">
        UPDATE mms_demand SET
            demand_no = #{demandNo},
            demand_type = #{demandType},
            source_order_no = #{sourceOrderNo},
            product_id = #{productId},
            product_code = #{productCode},
            product_name = #{productName},
            spec_model = #{specModel},
            unit = #{unit},
            demand_qty = #{demandQty},
            require_date = #{requireDate},
            priority = #{priority},
            customer_name = #{customerName},
            status = #{status},
            update_by = #{updateBy},
            update_time = #{updateTime},
            remark = #{remark}
        WHERE demand_id = #{demandId}
    </update>

    <delete id="deleteDemandByIds" parameterType="Long">
        UPDATE mms_demand SET del_flag = '2' WHERE demand_id IN
        <foreach collection="array" item="demandId" open="(" separator="," close=")">
            #{demandId}
        </foreach>
    </delete>
</mapper>
